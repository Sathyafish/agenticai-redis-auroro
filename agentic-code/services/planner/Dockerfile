FROM python:3.11-slim

WORKDIR /app
COPY services /app/services

RUN pip install --no-cache-dir -r /app/services/planner/requirements.txt

EXPOSE 8080
CMD ["uvicorn", "services.planner.main:app", "--host", "0.0.0.0", "--port", "8080"]
